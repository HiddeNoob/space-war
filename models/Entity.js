class Entity extends DrawableBase{
    /**
     * @param {Point} location
     * @param {Point[]} path 
     * @param {number} angle 
     * @param {string} color 
     */
    constructor(location, path, angle, color){
        super(location,path,angle,color);
    }
}